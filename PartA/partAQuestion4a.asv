function [w,y] = partAQuestion4a()
%set the function
g = @(x)sin(x) + (2*sin(3*x)*cos(x));
gP = @(x) cos(x) + (6*cos(x)*cos(3*x)) - (2*sin(x)*sin(3*x));
fp = 6 * pi;
n = 81;

%get w from centerDiff.m
w = centerDiff(g,n,fp);

%get y' from calculateY.m
y = calculateY(gP,n,fp);

plot(w
end
